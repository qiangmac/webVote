<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.kk.webvote.dao.UserMapper" >
    <resultMap id="BaseResultMap" type="com.kk.webvote.entity.User" >
        <id column="ID" property="id" jdbcType="INTEGER" />
        <result column="NAME" property="name" jdbcType="VARCHAR" />
        <result column="AGE" property="age" jdbcType="VARCHAR" />
        <result column="GENDER" property="gender" jdbcType="VARCHAR" />
    </resultMap>
    <sql id="Base_Column_List" >
        id, name, address
    </sql>
    <select id="selectUser" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from user
    </select>

    <insert id="addUser" parameterType="com.kk.webvote.entity.User">
        insert into t_test_user (id,name,age,gender)
        values (#{id,jdbcType=DECIMAL}, #{name, jdbcType=VARCHAR}, #{age,jdbcType=DECIMAL}, #{gender,jdbcType=VARCHAR})
    </insert>

    <select id="getAllUser" resultMap="BaseResultMap">
        select * from t_test_user
    </select>

    <select id="validateLoginUser">
        select * from t_test_user
        where login_id = #{loginId,jdbcType=VARCHAR} and password = #{password}
    </select>

</mapper>